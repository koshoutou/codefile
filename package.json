{
  "name": "codefile",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public --project-name codefile"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
